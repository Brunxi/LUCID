[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "sifi21-plus"
version = "0.1.0"
description = "Custom siFi21 fork used in LUCID"
authors = [{name = "Lucia Jimenez"}]
license = "MIT"

[tool.setuptools.packages.find]
where = ["."]
